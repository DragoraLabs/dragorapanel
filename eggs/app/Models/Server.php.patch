--- a/app/Models/Server.php
+++ b/app/Models/Server.php
@@ -1,6 +1,7 @@
 <?php
 
 namespace App\Models;
 
+use App\Models\Egg;
 use Illuminate\Database\Eloquent\Model;
 
 class Server extends Model
 {
-    protected $fillable = ['user_id', 'node_id', 'name', 'type', 'version', 'java_version', 'docker_image', 'status', 'memory_mb', 'storage_mb', 'port', 'ip_address', 'console_logs', 'command_queue', 'memory_used_mb', 'cpu_percent'];
+    protected $fillable = ['user_id', 'node_id', 'name', 'type', 'version', 'java_version', 'docker_image', 'status', 'memory_mb', 'storage_mb', 'port', 'ip_address', 'console_logs', 'command_queue', 'memory_used_mb', 'cpu_percent', 'egg_id'];

     protected $casts = [
         'port' => 'integer',
         'memory_mb' => 'integer',
         'storage_mb' => 'integer',
+        'memory_used_mb' => 'integer',
+        'cpu_percent' => 'float',
     ];

     public function user()
     {
         return $this->belongsTo(User::class);
     }
+
+    public function egg()
+    {
+        return $this->belongsTo(Egg::class);
+    }
 }
